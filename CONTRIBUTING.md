# 贡献指南

本文件说明提交改动前要读什么、怎么提交，以及提交代码时的授权约定。

## 提交前

- **门禁**：`bash tools/verify.sh` 全绿才提交（步骤以脚本为准）
- **代码规范 / 分支治理 / 构建顺序**：见 `AGENTS.md`（开发基线 `feat/dev`）
- **文档索引**：见 `docs/README.md`（唯一权威清单）

## 提交方式

- 分支：以 `feat/dev` 为基线，改动走 `feat/` `fix/` `exp/` 短命分支，完成后当天合流
- 提交信息：中文 Conventional Commits（例：`fix(viewer): 修正锚点偏移`）
- 一个提交一件事；改 `src/` 后必须重建产物

## 贡献授权

本仓库以 GPL-3.0 发布（见 `LICENSE`）。为提高代码的可用性与分发灵活性，**提交贡献即表示你同意以下约定**：

1. 你拥有所提交内容的版权，或已取得足以作出下述授权的许可；
2. 你向本项目维护者授予**非独占、永久、不可撤销、全球范围、免版税**的著作权许可，允许其使用、复制、修改、准备衍生作品并**再许可**，包括以与 GPL-3.0 并行的其他许可条款分发本项目；
3. 你保留自己贡献部分的版权，仍可自行以任何方式使用；
4. 提交内容若含第三方代码、图标、字体或素材，须在提交说明中标明来源与许可，并同步登记到 `docs/third-party.md`。

不接受上述约定时，请以 issue 形式提出建议，而不要提交代码。
